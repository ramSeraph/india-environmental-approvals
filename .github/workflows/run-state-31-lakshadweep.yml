name: Run Lakshadweep pipeline

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: state-pipeline-queue
  cancel-in-progress: false

jobs:
  run:
    uses: ./.github/workflows/run-state-pipeline.yml
    with:
      state_code: "31"
      state_name: "Lakshadweep"
