name: Run Chhattisgarh pipeline

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run:
    uses: ./.github/workflows/run-state-pipeline.yml
    with:
      state_code: "22"
      state_name: "Chhattisgarh"
