name: Run West Bengal pipeline

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: state-pipeline-queue
  cancel-in-progress: false

jobs:
  run:
    uses: ./.github/workflows/run-state-pipeline.yml
    with:
      state_code: "19"
      state_name: "West Bengal"
